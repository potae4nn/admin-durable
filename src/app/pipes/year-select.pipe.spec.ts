import { YearSelectPipe } from './year-select.pipe';

describe('YearSelectPipe', () => {
  it('create an instance', () => {
    const pipe = new YearSelectPipe();
    expect(pipe).toBeTruthy();
  });
});
