import { PaginationsPipe } from './paginations.pipe';

describe('PaginationsPipe', () => {
  it('create an instance', () => {
    const pipe = new PaginationsPipe();
    expect(pipe).toBeTruthy();
  });
});
