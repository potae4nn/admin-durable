import { CalpaginationsPipe } from './calpaginations.pipe';

describe('CalpaginationsPipe', () => {
  it('create an instance', () => {
    const pipe = new CalpaginationsPipe();
    expect(pipe).toBeTruthy();
  });
});
