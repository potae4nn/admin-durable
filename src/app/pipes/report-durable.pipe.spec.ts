import { ReportDurablePipe } from './report-durable.pipe';

describe('ReportDurablePipe', () => {
  it('create an instance', () => {
    const pipe = new ReportDurablePipe();
    expect(pipe).toBeTruthy();
  });
});
