import { ConvertYearPipe } from './convert-year.pipe';

describe('ConvertYearPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertYearPipe();
    expect(pipe).toBeTruthy();
  });
});
